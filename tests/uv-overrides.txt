# homeassistant pins lru-dict==1.3.0, which has no prebuilt wheel for
# Python 3.13 on any platform (source-only, needs a C compiler). 1.4.1 has
# full wheel coverage and is otherwise a drop-in replacement. Pass this to
# `uv pip install --override tests/uv-overrides.txt` to force it — see
# tests/README.md and CLAUDE.md's "Known platform gotchas".
lru-dict>=1.4.1

